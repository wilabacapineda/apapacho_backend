paths:
  /api/products:
    get:
      summary: lista todos los productos
      tags:
        - Products
      responses:
        "200":
          description: productos disponibles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Product'                
                  